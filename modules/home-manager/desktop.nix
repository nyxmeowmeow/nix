{
  xdg.desktopEntries = {
    melee = {
      name = "melee";
      comment = "melee bash script";
      exec = "/home/meow/nix/stuff/scripts"; # Command to run
      icon = "slippi-launcher"; # Should be in an icon theme or absolute path
      terminal = false; # Whether to launch in terminal
    };
  };
}
